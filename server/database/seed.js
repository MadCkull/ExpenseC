import db from './db.js';

export function seed() {
  console.log('Database initialized. No default users added (Fresh Start).');
  // No default users - Admin must add them manually per user request "start fresh".
}
